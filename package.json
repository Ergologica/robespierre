{
  "name": "robespierre",
  "version": "0.1.0",
  "description": "Robespierre — l'explorer che interpreta. Front-end per la blockchain Ergo.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check:ui": "node scripts/check-ui.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
